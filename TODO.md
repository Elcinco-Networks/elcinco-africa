# TODO - Fix subscribe button sizes

- [ ] Identify all “Subscribe” UI elements and newsletter forms across the site.
- [ ] Determine why subscribe buttons are inconsistent in size (CSS overrides, inline styles, missing button class rules).
- [ ] Implement consistent CSS for newsletter subscribe buttons (height/min-height + padding + box-sizing).
- [ ] Ensure the fix applies across all pages (index + any other page footers if they contain newsletter).
- [ ] Regression check: verify buttons render correctly on desktop and mobile breakpoints.

